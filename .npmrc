# Autenticação para publicação no registry npm.
# Em CI: NODE_AUTH_TOKEN é configurado automaticamente pelo setup-node (registry-url) + secret NPM_TOKEN.
# Localmente: export NODE_AUTH_TOKEN=<seu-token> antes de rodar lerna publish.
//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
