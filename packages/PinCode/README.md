# PinCode

A PIN code input component for entering numeric codes, typically for authentication.

## Installation

```bash
npm install @leroy-merlin-pt/pin-code
# or
yarn add @leroy-merlin-pt/pin-code
```

## Basic Usage

```vue
<template>
  <PinCode :length="6" v-model="pin" />
</template>
```
